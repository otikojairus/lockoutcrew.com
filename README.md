# Lockout Crew

Next.js pSEO site for locksmith, lockout, roadside, key cutting, and key replacement pages.

```bash
npm install
npm run dev
```

## Docker

Run the production container with:

```bash
docker compose up -d --build
```

The site will be available on `http://localhost:3000`.
